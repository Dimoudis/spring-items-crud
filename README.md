# spring-items-crud
A small Spring Boot application that manages items with basic CRUD operations. It exposes REST endpoints for adding items, showing all items, and searching items by code. The data is stored in a database using JPA.
